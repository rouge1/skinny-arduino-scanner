# Assigned numbers

Vendored from ble-scanner (tools/blescan/assigned/ at efcc2f0). Copies of the Bluetooth SIG's machine-readable assigned numbers, downloaded 2026-09-21 from
https://bitbucket.org/bluetooth-SIG/public (`assigned_numbers/`):

| File | Source path |
|---|---|
| `ad_types.yaml` | `core/ad_types.yaml` |
| `appearance_values.yaml` | `core/appearance_values.yaml` |
| `uri_schemes.yaml` | `core/uri_schemes.yaml` |
| `company_identifiers.yaml` | `company_identifiers/company_identifiers.yaml` |
| `service_uuids.yaml` | `uuids/service_uuids.yaml` |
| `member_uuids.yaml` | `uuids/member_uuids.yaml` |

To update, download the same paths again over these files. Each file carries the SIG's own
notice at its top.
